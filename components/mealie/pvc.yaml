apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mealie-data
  namespace: mealie
spec:
  storageClassName: cephfs-2026
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
